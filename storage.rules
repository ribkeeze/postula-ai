rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // CVs generados: cada usuario solo accede a los suyos
    match /cvs/{userId}/{fileName} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
